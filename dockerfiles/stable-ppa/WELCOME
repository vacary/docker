# FEniCS stable PPA version image

* This image is deprecated, please use the quay.io/fenicsproject/stable
image which is superior in both performance and features *

This image provides the most recent stable release of FEniCS
(<http://fenicsproject.org>). The FEniCS installation is installed via
the FEniCS PPA (<https://launchpad.net/~fenics-packages>).

Get started by solving a Poisson problem using FEniCS:
    
    cp /usr/share/dolfin/demo/documented/poisson/python/demo_poisson.py $HOME
    python demo_poisson.py
